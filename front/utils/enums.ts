 export enum Endpoint {
    // Auth
    REGISTER = "/api/register",
    LOGIN = "/api/login",
    LOGOUT = "/api/logout",
    ME = "/api/me",
    // Car
    CAR = "/api/car",
    SPEED = "/api/car/speed",
    DIR = "api/car/dir",
}