 export enum Endpoint {
    // Auth
    REGISTER = "/api/register",
    LOGIN = "/api/login",
    LOGOUT = "/api/logout",
    ME = "/api/me",
    // Car
    CAR = "/Car/info",
    LOCATION = "/getLocation",
    SPEEDUP = "/speedUp",
    SPEEDDOWN = "/speedDown",
    TURNLEFT = "/turnLeft",
    TURNRIGHT = "/turnRight",
}